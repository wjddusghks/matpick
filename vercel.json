{
  "installCommand": "cd matpick_all && corepack pnpm install --frozen-lockfile",
  "buildCommand": "cd matpick_all && corepack pnpm build",
  "outputDirectory": "matpick_all/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
